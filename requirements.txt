# --- Core data + numerics ---
# Versions that ship prebuilt wheels for Python 3.11, 3.12, AND 3.13
# on macOS arm64. Do NOT downgrade on 3.13; older releases lack wheels
# and pip falls back to source builds that fail without Xcode CLT.
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2

# --- Collaborative filtering ---
# 'implicit' provides a fast ALS implementation for implicit feedback.
# Chosen over Surprise (slow on 25M, RMSE-oriented) and TorchRec
# (heavier and not needed for this data size).
implicit==0.7.2

# --- Experiment tracking ---
# 2.19+ relaxes the pyarrow<18 cap. Required on Python 3.13 because
# pyarrow only ships 3.13 wheels from version 18 onward.
mlflow==2.19.0
pyarrow==18.1.0

# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- Frontend ---
streamlit==1.38.0
requests==2.32.3

# --- Persistence ---
joblib==1.4.2
